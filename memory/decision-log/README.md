# Decision Log

跨项目且已经确认的重要决策可在此登记。项目专属决策仍写入对应 `memory/project-memory/<project>/DECISIONS.md`。

只有 Accepted/Final 且带来源的 Human Decision 可以写入。若 Decision 与 `projects/<project-id>/project.yaml` 的当前正式状态冲突，Resolver 保留差异并以 Manifest 作为当前执行状态，等待人工完成一致性更新。
