interface:
  display_name: "Project Context Resolver"
  short_description: "Resolve project state and minimum task context"
  default_prompt: "Use $project-context-resolver to resolve this project, its current gate, and the next permitted action."
policy:
  allow_implicit_invocation: true
