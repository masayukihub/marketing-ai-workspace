interface:
  display_name: "内部模块｜Listing创意"
  short_description: "单张方向探索、深化和局部修改能力"
  default_prompt: "Use $jp-commerce-content-flow for user-facing work; invoke $amazon-listing-creative only as its internal creative module."
policy:
  allow_implicit_invocation: false
